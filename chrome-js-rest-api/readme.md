For automated testing with an external server
================================================================
For automated testing
The plugin sends the page HTML to the REST POST endpoint and then executes the JS code received from the endpoint
